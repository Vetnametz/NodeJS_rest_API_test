module.exports = function (app, router) {

    var ProfileController = require('./profile.controller'),
        settings = require('../../_global/config');

    //router.route('/user')
    //    .all(settings.checkUser)
    //    .get(ProfileController.oneUser);
};
